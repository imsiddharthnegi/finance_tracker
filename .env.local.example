# MongoDB connection string (optional - currently using file storage)
# Uncomment and update if you want to use MongoDB instead of file storage
# MONGODB_URI=mongodb://localhost:27017/finance-tracker

# For production deployment, use MongoDB Atlas or other cloud database
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/finance-tracker

# Next.js configuration
NEXT_PUBLIC_APP_NAME=Personal Finance Tracker
NEXT_PUBLIC_APP_VERSION=1.0.0

# Development settings
NODE_ENV=development

